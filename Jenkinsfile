pipeline {
    agent any
    stages {
        stage("Build"){
            steps{
                echo "========executing A========"
                sh "./gradlew assemble"
            }

        }
    }
}
